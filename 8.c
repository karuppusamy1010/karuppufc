#include <stdio.h>
int main()
{
    int num, j, s = 0;
    
    printf("Enter a positive integer: ");
    scanf("%d",&num);

    for(j=1; j <= n; ++j)
    {
        sum += j;   // sum = sum+i;
    }

    printf("Sum = %d",s);

    return 0;
}
